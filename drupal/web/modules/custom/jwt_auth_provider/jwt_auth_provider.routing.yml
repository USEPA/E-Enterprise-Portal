
jwt_auth_provider.jwt_auth_provider_configs:
  path: '/admin/config/jwt_auth_provider/jwtauthproviderconfigs'
  defaults:
    _form: '\Drupal\jwt_auth_provider\Form\JWTAuthProviderConfigs'
    _title: 'JWTAuthProviderConfigs'
  requirements:
    _permission: 'access administration pages'
  options:
    _admin_route: TRUE

