services:
  authentication.jwt_auth_provider:
    class: Drupal\jwt_auth_provider\Authentication\Provider\JWTAuthProvider
    arguments: ['@config.factory', '@entity.manager']
    tags:
      - {provider_id: 'jwt_auth_provider', name: authentication_provider, priority: 100 }
